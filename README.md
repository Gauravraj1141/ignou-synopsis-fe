FaceTrack Frontend (React)

- Set `REACT_APP_API_URL` in `.env` to point to backend API (e.g. http://localhost:8000/api)
- This is a skeleton with `Login` and `Dashboard` components. Integrate camera/face capture using WebRTC and send embeddings to `/api/attendance/mark/`.
